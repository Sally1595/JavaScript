let arr = ["This", "is", "a", "sentence."];

function printOutString() {
  // Join the array elements into a string using a space as a separator
  let result = arr.join(" ");
  
  // Print out the resulting string
  console.log(result);
}

printOutString();
